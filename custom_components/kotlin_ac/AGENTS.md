# Home Assistant integration distribution

- This directory is mechanically synchronized from the private source release.
- Preserve asynchronous I/O, atomic intent updates, finite timeouts, and revision protection.
- Do not hand-edit generated copies here; fix and test the private source, then publish a release.
- `manifest.json` version must match the app version and public Git tag.

